/*
 * Deployment configuration
 *
 * Keep the project path in this value for GitHub Pages project-site deploys.
 * For a custom domain or user site, use its origin without a trailing slash.
 */
window.LUMOS_SITE_CONFIG = {
  siteUrl: "https://lumos-2005.github.io/lumos-translator"
};
